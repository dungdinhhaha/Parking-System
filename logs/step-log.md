# Step Log

## 2026-06-05

- Created the Spring Boot scaffold under `D:\java\parking-system`.
- Implemented the core domain model for the parking system.
- Added JPA entities for users, vehicles, buildings, floors, zones, slots, sessions, reservations, payments, policies, incidents, plate recognition logs, and feedback.
- Added the required enum types for roles, statuses, payment, policy, incident, feedback, and recognition flow.
- Replaced repository stubs with `JpaRepository` interfaces.
- Added `BaseEntity` with `id`, `createdAt`, and `updatedAt` timestamp handling.
- Added explicit column names for the main unique keys to keep the schema stable.
- Reworked `BusinessException` into a proper runtime exception.
- Switched datasource configuration from H2 to Supabase PostgreSQL via environment variables.
- Added `application-test.yml` so tests can run with H2 without needing Supabase credentials.
- Added `prepareThreshold=0` to the Supabase JDBC URL for pooler compatibility.
- Removed H2 dependency and deleted the H2-backed test profile as requested.
- Added `.env.example` and an IntelliJ run configuration template for Supabase.
- Added `spring.config.import=optional:file:.env[.properties]` so the app can read a root `.env` file directly.
- Removed JWT and switched auth to session-based Spring Security login.
- Added role-based authorization for MANAGER, STAFF, DRIVER, and SYSTEM_ADMIN.
- Added `/api/auth/test/**` endpoints and `/auth-test.html` for session auth testing.
- Configured Lombok annotation processing in Maven and fixed Spring Security 7 `DaoAuthenticationProvider` wiring.
- Verified full `src/main/java` compile with `javac` and Lombok annotation processing enabled.
- Added startup seed data for auth users: manager, staff, driver, and system admin.
- Added startup migration for legacy `users_role_check` values so Supabase accepts the new role names.
- Implemented infrastructure management CRUD for buildings, floors, zones, and slots with role protection.
- Added seed data for one building, two floors, three zones, and seven slots.
- Added `/infra-test.html` for quick manual testing of infrastructure CRUD APIs.
- Added global exception handling and duplicate checks so infrastructure API errors return `400/409` instead of `500`.
- Hardened infrastructure seed lookup to tolerate duplicate legacy buildings without crashing startup.
- Added `buildingCode` as the unique business key for buildings and introduced Flyway migrations for schema management.
- Added `spring-boot-starter-flyway` so migrations run before Hibernate validation.
- Added `org.flywaydb:flyway-database-postgresql` so Flyway can recognize PostgreSQL 17.x on Supabase.
- Implemented reservation create/cancel flow with car slot holds and motorbike zone reserved counts, without upfront payment.
- Added extra motorbike zone/slots for reservation testing and created `/reservation-test.html`.
- Added validation error handling for reservation input and auto-filled future start/end times in the test page.
- Switched reservation time validation to `@FutureOrPresent` and made the test page list available slots/zones by `buildingCode`.
- Added a reservation availability endpoint and hardened Spring Security to return proper `401/403` JSON instead of surfacing `500 Access Denied`.
- Updated the reservation test page to hide manager-only actions from driver users.
- Refactored reservation into `ReservationFacade`, validator, allocation strategies, resource manager, mapper, and availability query service to reduce `ReservationServiceImpl` responsibilities.
- Implemented reservation-first allocation strategies for check-in: factory selection by `vehicleType`, reservation lookup before greedy fallback, car nearest-slot greedy, and motorbike max-capacity greedy.
- Standardized API responses with `ApiResponse<T>` and `ApiErrorResponse`, and updated auth, infrastructure, reservation, and auth-test endpoints/UI to use the common envelope.
- Implemented the check-in flow with `ParkingSessionController`, `ParkingOperationFacade`, local plate-image storage, mock plate recognition, and session creation wired through allocation strategy.
- Added a mock plate-recognition API at `/api/ai/plate-recognition/mock` with a dedicated response DTO for testing AI plate number extraction without a real model.
- Implemented Pricing/FeeCalculation using a strategy registry pipeline: hourly, overnight, lost-ticket, and wrong-zone policies; added pricing CRUD APIs, fee calculation API, and seed data for default pricing policies.
- Implemented Payment using `PaymentProcessorFactory` plus adapter-based processors: cash checkout now creates payment records, marks sessions complete on success, and leaves clear extension points for Momo/VNPay later.
- Implemented Check-out using a facade plus small checkout services: active-session lookup, optional plate re-check with mock AI, fee calculation, payment processing, session completion, and resource release for car slots and motorbike zone counts.
- Added `session-flow-test.html` for end-to-end manual testing of check-in and check-out with session login, optional plate-image upload, and auto-fill of the checkout session id from the last check-in response.
- Added a quick sample check-in action to `session-flow-test.html` so the page can auto-fill a demo vehicle and trigger check-in immediately for faster manual testing.
- Implemented Incident handling with `ParkingIncidentService`, `IncidentFacade`, incident validator/mapper, and incident rules for lost ticket, wrong plate, overdue, wrong zone, unpaid, and generic cases.
- Added incident APIs for create, list, query by session, query by status, and resolve/close, with session state updates driven by incident type.
- Implemented report endpoints for dashboard, revenue, occupancy, and incidents using read-only report calculators over sessions, reservations, payments, zones, slots, and incidents.
- Added business-oriented unit test coverage for reservation create/cancel, check-in regular and reservation-backed flows, checkout success/failure/wrong plate, and payment success/failure paths using Mockito-based facade/service tests.
- Removed feedback from the active delivery scope and documented the remaining backend scope as user management and vehicle management only.
- Implemented user management APIs for create, update, get, list, lock/unlock, and password reset.
- Implemented vehicle management APIs for create, update, get, list, and delete with owner scoping for drivers and admin-level access for managers/system admins.
- Added browser-based manual test pages for user management, vehicle management, payment, incident, report, and mock AI, plus a `/test-hub.html` entry point for all feature tests.
- Added `FRONTEND_AI_HANDOFF.md` to document backend setup, API contracts, role expectations, frontend scope, and AI integration responsibilities.
- Added `FRONTEND_CHECKLIST.md`, `AI_CHECKLIST.md`, and `ROUTE_ROLE_MATRIX.md` as short handoff documents for the frontend and AI teams.
